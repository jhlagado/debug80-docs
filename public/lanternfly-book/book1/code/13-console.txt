// console.lafy — the platform interface module for the fictional LF-1
// teaching machine. The module assembly defines the symbol the external
// binding names; a real platform module does the same from a datasheet.
asm
ROM_VBLANK_WAIT = $0f09
end

export extern sub writeUnsigned(value as u16)
export extern sub waitForVBlank() from "ROM_VBLANK_WAIT"
