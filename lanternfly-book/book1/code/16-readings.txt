// readings.lafy — an ordinary module exporting the measurement model.
export var previousReading as u8 = 250
export var currentReading as u8 = 20
export var change as u16 = 0

export sub measureChange()
    change = abs(currentReading - previousReading)
end
